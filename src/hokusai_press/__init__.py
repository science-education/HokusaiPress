"""HokusaiPress — scan PDF to clean, light, searchable e-book PDF.

Non-destructive, parameter-driven pipeline: analysis records parameters, the
final renderer produces output pixels in a single resampling pass from the
original image. Batch-first, with a review queue for low-confidence pages.
"""

__version__ = "0.0.1"
